import 'package:flutter/material.dart';

class TeamSettingsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Team Settings Page'),
      ),
      body: Center(
        child: Text('Team Settings Content Here'),
      ),
    );
  }
}
