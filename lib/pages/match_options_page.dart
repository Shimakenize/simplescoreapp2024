import 'package:flutter/material.dart';

class MatchOptionsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Match Options Page'),
      ),
      body: Center(
        child: Text('Match Options Content Here'),
      ),
    );
  }
}
